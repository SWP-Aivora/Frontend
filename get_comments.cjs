const https = require('https'); https.get('https://api.github.com/repos/SWP-Aivora/Frontend/pulls/30/comments', { headers: { 'User-Agent': 'Node.js', 'Accept': 'application/vnd.github.v3+json' } }, (res) => { let data = ''; res.on('data', c => data += c); res.on('end', () => console.log(JSON.parse(data).map(c => c.path + ':' + c.line + ' - ' + c.body))); }).on('error', console.error);  
